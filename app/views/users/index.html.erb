<% provide(:tytle, 'All Users') %>

<div class="col-md-10 col-md-offset-1">
  <table class="table table-condensed table-hover" id="table-users">
    <%= will_paginate %>
    <thead>
      <tr>
        <th><%= User.human_attribute_name :name %></th>
      </tr>
    </thead>
    
    <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.name, user %></td>
    </tr>
    <% end %>
  </table>
  <%= will_paginate %>
</div>